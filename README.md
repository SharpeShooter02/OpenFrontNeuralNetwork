# OpenFrontNN

Reinforcement-learning experiments on the [OpenFront](https://github.com/openfrontio/OpenFrontIO) RTS engine.

The engine is pinned as a git submodule under `vendor/OpenFrontIO`. Our code lives in `src/`.

## Setup
```
git clone --recurse-submodules <this repo>
npm install
npm run harness
```
